local React = require(script.Parent.Parent.Parent.React)

local e = React.createElement

--[=[
	@within Components
	@unreleased
]=]
local function FloatingFocusManager()
	return e("", {})
end

return FloatingFocusManager
